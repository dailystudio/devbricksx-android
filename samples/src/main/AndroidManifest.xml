<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:name=".SamplesApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:screenOrientation="sensor"
        android:theme="@style/Theme.Samples">

        <activity
            android:exported="true"
            android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".quickstart.CaseActivity"/>
        <activity android:name=".inmemory.CaseActivity"/>
        <activity android:name=".imagematrix.CaseActivity"/>
        <activity android:name=".viewpager.CaseActivity"/>
        <activity android:name=".fragmentpager.CaseActivity"/>
        <activity android:name=".datachanges.CaseActivity"/>
        <activity android:name=".imagemask.CaseActivity"/>
        <activity android:name=".customadapter.CaseActivity"/>
        <activity android:name=".settings.normal.CaseActivity"/>
        <activity android:name=".settings.dialog.CaseActivity"/>
        <activity android:name=".nonrecyclablelistview.CaseActivity"/>
        <activity android:name=".paging.CaseActivity"/>
        <activity android:name=".jackandjill.CaseActivity"/>

    </application>

</manifest>